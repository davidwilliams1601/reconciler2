{
  "name": "invoice-reconciliation-backend",
  "version": "1.0.0",
  "description": "Backend for Invoice Reconciliation Application",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "cd ../frontend && npm install && npm run build && cd ../backend",
    "postbuild": "mkdir -p build && cp -r ../frontend/build ."
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
} 